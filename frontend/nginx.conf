server { listen 8080; server_name _; root /usr/share/nginx/html; location = /health { access_log off; return 200 'ok'; add_header Content-Type text/plain; } location /vr/sales/ { try_files $uri $uri/ /vr/sales/index.html; } }
