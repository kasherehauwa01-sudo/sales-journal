import {BarChart3,FileUp,Menu,ReceiptText,Settings,X} from 'lucide-react';import {useState} from 'react';import {NavLink,Outlet} from 'react-router-dom';
export function Layout(){const [open,setOpen]=useState(false);const links=[['/',ReceiptText,'Журнал продаж'],['/analytics',BarChart3,'Аналитика'],['/imports',FileUp,'Импорт'],['/settings',Settings,'Настройки'] ] as const;return <div className="app"><aside className={open?'open':''}><div className="brand"><span>ЖП</span><div>Журнал продаж<small>Управление данными</small></div><button onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(([to,Icon,label])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}</nav><footer>Production · PostgreSQL</footer></aside><main><header><button className="mobile" onClick={()=>setOpen(true)}><Menu/></button><div><b>Журнал продаж</b><small>Единый реестр и аналитика</small></div></header><Outlet/></main>{open&&<div className="backdrop" onClick={()=>setOpen(false)}/>}</div>}
