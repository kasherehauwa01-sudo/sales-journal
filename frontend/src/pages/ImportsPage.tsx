import {CheckCircle2,FileSpreadsheet,RefreshCw,UploadCloud} from 'lucide-react';import {useEffect,useRef,useState} from 'react';import {api} from '../api/client';import {Empty,ErrorBox} from '../components/States';import type {ImportBatch} from '../types';import {date,datetime,number} from '../utils/format';
export function ImportsPage(){const [items,setItems]=useState<ImportBatch[]>([]);const [files,setFiles]=useState<File[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const input=useRef<HTMLInputElement>(null);const load=()=>api<ImportBatch[]>('/imports').then(setItems).catch(e=>setError(e.message));useEffect(()=>{load();const timer=setInterval(load,5000);return()=>clearInterval(timer)},[]);async function upload(){if(!files.length)return;setBusy(true);setError('');const body=new FormData();files.forEach(f=>body.append('files',f));try{await api('/imports',{method:'POST',body});setFiles([]);if(input.current)input.current.value='';load()}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <div className="page"><div className="page-title"><div><h1>Импорт данных</h1><p>Загрузка продаж из файлов XLS и XLSX</p></div></div>{error&&<ErrorBox text={error}/>}<section className="panel upload"><div className="drop" onClick={()=>input.current?.click()}><UploadCloud size={38}/><b>{files.length?`Выбрано файлов: ${files.length}`:'Выберите файлы или перетащите сюда'}</b><span>XLS/XLSX, исходные файлы сохраняются на сервере</span><input ref={input} hidden multiple type="file" accept=".xls,.xlsx" onChange={e=>setFiles(Array.from(e.target.files||[]))}/></div>{files.map(f=><div className="file" key={f.name}><FileSpreadsheet/><span><b>{f.name}</b><small>{number(f.size/1024)} КБ</small></span></div>)}<button className="primary" disabled={!files.length||busy} onClick={upload}>{busy?<RefreshCw className="spin"/>:<UploadCloud/>}{busy?'Загрузка…':'Начать импорт'}</button><p className="hint">Обработка идёт в фоне. Ошибка одной строки не останавливает импорт остальных данных.</p></section><div className="section-title"><h2>История импортов</h2><button onClick={load}><RefreshCw size={16}/>Обновить</button></div><section className="panel">{!items.length?<Empty text="Импортов пока нет"/>:<div className="table-wrap"><table><thead><tr><th>Файл</th><th>Загружен</th><th>Период</th><th>Строк</th><th>Добавлено</th><th>Дубли</th><th>Ошибки</th><th>Статус</th><th>Время</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.filename}</b><small>{number(x.file_size/1024)} КБ</small></td><td>{datetime(x.uploaded_at)}</td><td>{x.period_start?`${date(x.period_start)} — ${date(x.period_end)}`:'—'}</td><td>{number(x.total_rows)}</td><td>{number(x.added_rows)}</td><td>{number(x.duplicate_rows)}</td><td title={x.errors.map(e=>`Строка ${e.row_number}: ${e.message}`).join('\n')} className={x.error_rows?'danger':''}>{number(x.error_rows)}</td><td><span className={`status ${x.status}`}>{x.status==='completed'?<CheckCircle2/>:<RefreshCw/>}{({queued:'В очереди',processing:'Обработка',completed:'Готово',failed:'Ошибка'} as Record<string,string>)[x.status]||x.status}</span></td><td>{x.duration_ms?`${number(x.duration_ms/1000)} с`:'—'}</td></tr>)}</tbody></table></div>}</section></div>}
