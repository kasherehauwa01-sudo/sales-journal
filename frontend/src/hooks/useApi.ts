import {useEffect,useState} from 'react';import {api} from '../api/client';
export function useApi<T>(path:string){const [data,setData]=useState<T>();const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{let active=true;setLoading(true);setError('');api<T>(path).then(x=>{if(active){setData(x);setError('')}}).catch(e=>{if(active)setError(e instanceof Error?e.message:'Не удалось загрузить данные')}).finally(()=>active&&setLoading(false));return()=>{active=false}},[path]);return {data,loading,error}}
